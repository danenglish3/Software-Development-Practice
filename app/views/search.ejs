<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title>Search</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="../public/css/main.css" />
    <script src="../public/js/main.js"></script>
</head>

<body>
        <h1>Search Page</h1>
    <!--Upon Submitting the form, make a request with the post method to url /new-listing-->
    <form method="post" action="/search" enctype="multipart/form-data" id="newSearchRequest">
        <label>Keyword Search</label>
        <br>
        <input type="text" name="searchKeywords">
        <br>
        <label>Location</label>
        <br>
        <input type="text" name="searchLocation">
        <br>
        <label>Catergory</label>
        <br>
        <input type="text" name="searchCategory">
        <br>
        <input type="submit" value="Submit">
    </form>

    <table>
            <% for(var i=0; i < searchResults.length; i++) { %>
               <tr>
                 <td><%= searchResults[i].name %></td>
                 <td><%= searchResults[i].serviceID %></td>
                 <td><%= searchResults[i].category %></td>
                 <td><%= searchResults[i].location %></td>
                 <td><%= searchResults[i].description %></td>
                 <td><%for (var image in searchResults[i].imageFiles){%>
                        <img src="/temp/<%= searchResults[i].imageFiles[image] %>" alt="" height="100">
                    <%}%></td>
               </tr>
            <% } %>
    </table>    
</body>

</html>