<!DOCTYPE html>
<html>

<head>
    <% include ../partials/head %>
</head>

<body>
    <header>
        <% include ../partials/header %>
    </header>
    <div class="center">
        <h1><b>Search Results</b></h1>
    </div>
    <% if( listingResults.length > 0 ) { %>
    <% var i = 0 %>
    <table>
        <% while( i < listingResults.length ) { %>
        <tr>
            <% for( var j = 0; j < 3; j++ ) { %>
            <td>
                <div class="card-deck">
                    <div class="card" style="width: 20rem;">
                        <img class="card-img-top" src="/temp/<%= listingResults[i]singleListing.imageFile %>" alt="Service Image"
                            height="200">
                        <div class="card-body">
                            <h2 class="card-title">
                                <%= listingResults[i].singleListing.name %>
                            </h2>
                            <div class="card-text">
                                <%= listingResults[i].singleListing.location %>
                            </div>
                            <p class="card-text">
                                <%= listingResults[i].singleListing.description %>
                            </p>
                            <form method="get" action="/profile/<%= listingResults[i].singleListing.profileid %>"
                                enctype="multipart/form-data" id="selectProfile">
                                <input type="submit" value="Go to Profile &rarr;">
                            </form>
                            <p></p>
                            <form method="get" action="/new_quote/<%= listingResults[i].singleListing.serviceid %>"
                                enctype="multipart/form-data" id="newQuote">
                                <input type="submit" value="Request a Quote">
                            </form>
                        </div>
                        <div class="card-footer text-muted">
                        </div>
                    </div>
                </div>
            </td>
            <% i++ %>
            <% } %>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    <h1>Unable to find anything with that criteria, Please try again!</h1>
    <% } %>

    <p></p>
    <form method="get" action="/search" enctype="multipart/form-data" id="backToSearch">
        <input type="submit" value="Back to search">
    </form>

    <footer class="footer">
        <% include ../partials/footer %>
    </footer>
</body>

</html>